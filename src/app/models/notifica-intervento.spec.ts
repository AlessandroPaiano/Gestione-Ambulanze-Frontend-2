import { NotificaIntervento } from './notifica-intervento';

describe('NotificaIntervento', () => {
  it('should create an instance', () => {
    expect(new NotificaIntervento()).toBeTruthy();
  });
});
