import { AmbulanzaDisponibile } from './ambulanza-disponibile';

describe('AmbulanzaDisponibile', () => {
  it('should create an instance', () => {
    expect(new AmbulanzaDisponibile()).toBeTruthy();
  });
});
